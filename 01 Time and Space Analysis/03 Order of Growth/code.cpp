#include<bits/stdc++.h>
using namespace std;

int main()
{
	/*

	Order of Growth:

	1.Used to compare which function is growing faster than other.
	2.lim n->INF g(n)/f(n) gives 0.
	this means f(n) is growing faster than g(n), n,f(n),g(n)>=0

	Example:

	f(n)=n*n
	g(n)=n

	so lim n->INF g(n)/f(n) = 1/n = 0 

	so, f(n) grows faster than g(n)

	*/
	
	return 0;
}