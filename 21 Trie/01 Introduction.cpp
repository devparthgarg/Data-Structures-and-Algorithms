#include<bits/stdc++.h>
using namespace std;

int main()
{
	/*
	
-->	Efficient for following 
	operations on words of dictionary:

	1. search
	2. insert
	3. delete
	4. prefix search
	5. lexiographical ordering of words

-->	Comparsion of Trie and Hashing

	
	Function			|	Trie						|	Hashing
-----------------------------------------------------------------------------	
	search				|	O(word_len) worst case		|	O(word_len) average case

	insert				|		"						|		"

	delete				|		"						|		"

	prefix search		|	O(word_len+output_len)		|	Not Supported

	lexiographical		|	O(output_len)				|		"
	ordering			|								|

	*/
	return 0;
}