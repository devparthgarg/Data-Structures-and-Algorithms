#include<bits/stdc++.h>
using namespace std;

int main()
{
	/*
	
	Red Black Tree:

	--> Every node is either black or red

	--> Root node is always black

	--> No two consecutive Red nodes

	--> Number of black nodes from every 
	node to all of its descedants leaves 
	should be same.

	--> No very strict as AVL tree so,
	less number of rotations

	--> AVL Tree has faster search operations
	compared to Red Black Tree.

	--> When having a mix of operations (insertions,
	deletions,search) prefer Red Black Tree.

	*/
	return 0;
}