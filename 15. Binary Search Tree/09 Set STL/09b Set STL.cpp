#include<bits/stdc++.h>
using namespace std;

int main()
{
	/*

	Internal Working and Time Complexities:

	--> set is build on self balancing BSTs 
	
	--> particularly Red Black Tree
	
	--> inorder traversal of BST is sorted

	--> begin(),end(),rbegin(),rend(),size(),empty() : O(1) 

	--> insert(),find(),count(),lower_bound(),upper_bound(),erase(val) : O(logN) 

	--> erase(it) : Amortized O(1) operation

	*/
	
	return 0;
}